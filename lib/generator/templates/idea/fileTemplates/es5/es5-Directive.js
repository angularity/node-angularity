/**
 * ${NAME} Directive
 * Created by ${USER} on ${DATE}.
 * @ngInject
 */
function ${NAME}() {
    'use strict';

    return {
        transclude: false,
        scope: {},
        link: link
    };
}

function link(${DS}element, ${DS}attributes, ${DS}scope){

}

module.exports = ${NAME};
